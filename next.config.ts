import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // Hide the Next.js "N" corner indicator in screenshots / local preview.
  devIndicators: false,
};

export default nextConfig;
